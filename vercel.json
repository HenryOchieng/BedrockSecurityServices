{
    "builds":[{
        "src": "Bedrock/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.8.7"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Bedrock/wsgi.py"
        }
    ]
}